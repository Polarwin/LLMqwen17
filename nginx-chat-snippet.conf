    # chat-begin (static chat UI for the local LLM, served from /opt/llm/chat)
    location = /chat { return 302 /chat/; }
    location /chat/ {
        alias /opt/llm/chat/;
        index index.html;
    }
    # chat-end
